#!/usr/bin/env python
"""Orchestration placeholder.

For reproducibility, prefer running the three explicit stages separately so the
Stage-1 checkpoint and HKG artifact are inspectable.
"""

print("Run scripts/train_stage1.py, scripts/build_hkg.py, then scripts/train_stage2.py")
