# Notebooks

`run_stage1.ipynb` is the single Stage-1 run notebook.  It does not contain a custom training loop; every Stage-1 run is invoked through:

```bash
python scripts/train_stage1.py
```

`aog_hkg_diagnostics.ipynb` is for Stage-2/HKG diagnostics after training.
